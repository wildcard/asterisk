{
  "name": "@asterisk/extension",
  "version": "0.1.0",
  "description": "Asterisk browser extension for form detection",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build && cp public/manifest.json dist/manifest.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@asterisk/core": "workspace:*"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.3.0",
    "@types/chrome": "^0.1.36",
    "typescript": "^5.7.2",
    "vite": "^7.3.1"
  }
}
